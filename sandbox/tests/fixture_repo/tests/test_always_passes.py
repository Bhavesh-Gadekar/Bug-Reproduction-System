"""This test ALWAYS PASSES — gives pytest something to report alongside the failure."""


def test_always_passes():
    """Trivially true assertion — confirms pytest itself is working."""
    assert 1 == 1
